import { createSlice } from '@reduxjs/toolkit'

export const {{camelCase name}}Slice = createSlice({
  name: '{{camelCase name}}',
  initialState: { },
  reducers: {
    // add{{pascalCase name}}: state => {  },
    // delete{{pascalCase name}}: state => { },
    // update{{pascalCase name}}: (state, action) => {    }
  }
})

// export const { add{{pascalCase name}}, delete{{pascalCase name}}, update{{pascalCase name}} } = {{camelCase name}}Slice.actions

export default {{camelCase name}}Slice.reducer